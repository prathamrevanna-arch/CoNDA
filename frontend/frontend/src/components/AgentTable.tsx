import type { AgentRow } from '@frontend/types';
export function AgentTable({ rows }: { rows: AgentRow[] }) { return <div className="table-scroll"><table><thead><tr><th>Agent</th><th>Last price</th><th>PnL</th><th>Pair risk</th><th>Side</th></tr></thead><tbody>{rows.map((row) => <tr key={row.agent_id}><td><span className="agent-dot" />{row.agent_id}</td><td>${row.lastPrice.toFixed(2)}</td><td className="positive">+${row.pnl.toFixed(2)}</td><td><span className={`risk-pill ${row.risk >= 70 ? 'high' : row.risk >= 40 ? 'mid' : 'low'}`}>{row.risk}</span></td><td className="muted">{row.side}</td></tr>)}</tbody></table></div>; }
